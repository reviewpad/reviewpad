name: Reviewpad

on:
  pull_request:
  workflow_run:
    workflows: ["*"]
    types:
      - completed

jobs:
  reviewpad:
    runs-on: ubuntu-latest
    permissions: write-all
    steps:
      - name: Reviewpad
        uses: reviewpad/action@debug
